[build]
  command = "npm ci && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"
  
[[headers]]
  for = "/data/intervenciones.json"
  [headers.values]
    Cache-Control = "max-age=3600"
    